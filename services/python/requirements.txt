# FastAPI service
fastapi==0.111.0
uvicorn[standard]==0.30.1
httpx==0.27.0

# Beat / downbeat detection. BeatNet pulls madmom + librosa + torch.
# madmom 0.16.1 is the last release and builds its Cython extensions against
# numpy<1.24 (installed ahead of this file in the Dockerfile).
BeatNet==1.1.0
madmom==0.16.1
librosa==0.9.2
soundfile==0.12.1
# BeatNet imports pyaudio unconditionally (realtime mic mode); we never use it,
# but the import must resolve. Needs portaudio19-dev (installed in the Dockerfile).
pyaudio==0.2.14
